import { createSlice } from "@reduxjs/toolkit";
import type { PayloadAction } from "@reduxjs/toolkit";

const homeitemSlice = createSlice({name:'homeitem',initialState:[],reducers:{

}})
export default homeitemSlice.reducer;